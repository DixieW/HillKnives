node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db
desktop.ini

# Env — NEVER commit real secrets
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Large media files (store separately, e.g. Git LFS or CDN)
*.mp4
*.mov
*.avi
*.webm

# Claude Code local config (contains machine-specific paths and permissions)
.claude/settings.local.json

# Keys and certificates — should never end up in source control
*.pem
*.key
*.p12
*.pfx
*.cer
*.crt

# Editor directories
.idea/
.vscode/settings.json
